version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  pre_build:
    commands:
      - aws ecr get-login-password --region ap-northeast-1 | docker login --username AWS --password-stdin 189484286514.dkr.ecr.ap-northeast-1.amazonaws.com
      - REPOSITORY_URI=${IMAGE_URI}
  build:
    commands:
      - docker build -t ${IMAGE_URI}:latest .
  post_build:
    commands:
      - docker push ${IMAGE_URI}:latest
artifacts:
  files: output.json